POSTGRES_USER=postgres
POSTGRES_PASSWORD=dev
POSTGRES_DB=tasks

# Where the app looks for the database. Running the app on your machine, that is the
# port the container publishes; inside compose, the api service overrides it with db:5432.
DATABASE_URL=postgresql://postgres:dev@localhost:5432/tasks
REDIS_URL=redis://localhost:6379
